name: longevity_compass
description: Flutter client for the Longevity Compass MVP.
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.2.0
  cupertino_icons: ^1.0.8
  firebase_analytics: ^12.2.0
  firebase_auth: ^6.3.0
  firebase_core: ^4.6.0
  firebase_crashlytics: ^5.1.0
  firebase_messaging: ^16.1.3
  firebase_remote_config: ^6.3.0
  google_fonts: ^8.0.2
  http: ^1.6.0
  intl: ^0.20.2
  provider: ^6.1.5+1

flutter:
  uses-material-design: true
